server:
  port: 8888

# 이전 버전
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PairMate/config-repo
          # default-label: main
          default-label: develop1
          search-paths:
            - .
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}